# Participant_Package/

Drop the organizer-provided files here once we have them:
`questions_template.json`, `answer_template.json`, `validate.json`, and
`public_questions.jsonl` (15 calibration questions with graded expected
facts). See `../../NOTES.md` → "Blocked on".
