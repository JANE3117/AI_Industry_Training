# training/

Fine-tuning scripts, config/hyperparameters, checkpoints (or pointers to
them), and the base-vs-fine-tuned Nemotron comparison evidence go here.
`llm-eval-nim-demo/` elsewhere in this repo has a working NeMo AutoModel
(LoRA) + NeMo Evaluator SDK pattern to adapt — see `../../NOTES.md`.
