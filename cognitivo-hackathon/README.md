# Cognitivo Hackathon — Market Signal Agent

Prep folder for a new, separate competition: the **Cognitivo Hackathon** —
"Build and fine-tune an evidence-grounded market signal agent over RBA,
ASX, and AFR data." Unrelated to `furniture-app/` elsewhere in this repo;
this is a different event in the same AI Industry Training program.

## Status: Day 0 — planning only

The challenge brief (`AI Training and Hackathon_ Event Portal.pdf`, exported
2026-07-30) has been read and distilled into [NOTES.md](NOTES.md). Nothing
has been built yet — real implementation is blocked on organizer material
that isn't in this repo (dataset schemas, model-serving endpoints, the
question/answer templates). See "Blocked on" in NOTES.md for the exact list.

`TeamSubmission/` below is a placeholder matching the folder structure the
brief requires (`README.md`, `submission.json`, `src/`, `training/`,
`logs/`, `Participant_Package/`) — each subfolder currently just explains
what belongs in it. Before this can actually be submitted, it also needs to
become (or be pushed to) its own **public GitHub repository** at a pinned
commit — that repo doesn't exist yet.

## Read next

- [NOTES.md](NOTES.md) — full distillation: architecture, scoring, traps,
  fact-checked model details, and exactly what's still missing.
- [TeamSubmission/README.md](TeamSubmission/README.md) — draft of the
  actual deliverable README.
